import styled from 'styled-components'

export const HeadingContainer = styled.div`
  .delete-button {
    visibility: hidden;
  }

  :hover {
    .delete-button {
      visibility: visible;
    }
  }
`
